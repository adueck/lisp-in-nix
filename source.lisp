(let
  (
   (abC (+ 10 1))
   (myf (lambda x (* x 10)))
  )
  (myf 2))
; basic LISP syntax for calculations
; (let
;   ; declare variables
;   ((x 1) (y 2))
;   (* 
;     (+ x y #| inline comments too |# )
;     10))
